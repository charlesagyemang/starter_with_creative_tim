<nav class="sidenav navbar navbar-vertical  fixed-left  navbar-expand-xs navbar-light bg-white" id="sidenav-main">
  <div class="scrollbar-inner">
    <!-- Brand -->
    <div class="sidenav-header  align-items-center">
      <a class="navbar-brand" href="<%= root_path %>">
        <h1>NBKFund</h1>
      </a>
    </div>
    <div class="navbar-inner">
      <!-- Collapse -->
      <div class="collapse navbar-collapse" id="sidenav-collapse-main">
        <!-- Nav items -->
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link <%= "#{controller_name}/#{action_name}".eql?('cockpit/admin') ? 'active': '' %>" href="<%= cockpit_admin_path %>">
              <i class="ni ni-tv-2 text-primary"></i>
              <span class="nav-link-text">Dashboard</span>
            </a>
          </li>

          <li class="nav-item">
            <a class="nav-link <%= "#{controller_name}/#{action_name}".eql?('cockpit/admin') ? 'active': '' %>" href="<%= cockpit_admin_path %>">
              <i class="ni ni-briefcase-24 text-yellow"></i>
              <span class="nav-link-text">Investors</span>
            </a>
          </li>

          <li class="nav-item">
            <a class="nav-link <%= "#{controller_name}/#{action_name}".eql?('cockpit/admin') ? 'active': '' %>" href="<%= cockpit_admin_path %>">
              <i class="ni ni-credit-card text-blue"></i>
              <span class="nav-link-text">Contributions</span>
            </a>
          </li>

          
          <li class="nav-item">
            <a class="nav-link <%= "#{controller_name}/#{action_name}".eql?('cockpit/admin') ? 'active': '' %>" href="<%= cockpit_admin_path %>">
              <i class="ni ni-bold-right text-green"></i>
              <span class="nav-link-text">Payouts</span>
            </a>
          </li>

          
        </ul>
  
      </div>
    </div>
  </div>
</nav>
