<div class="container-fluid">
<br />
    <%= link_to 'Back', "/loans?loaner=#{@loaner.id}" %>  <br /><br />

    <h1>Add New Loan For <%= @loaner.full_name %></h1>
</div>


<%= render 'form', loan: @loan %>
<%= render 'loans_table', loans: @all_individual_loans %>

